{
  "name": "oreos",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.3.0",
    "react-dom": "^16.3.0"
  },
  "cacheDirectories": ["node_modules", "client/node_modules"],
  "scripts": {
    "start":
      "concurrently \"cd server && nodemon index.js\" \"cd client && npm start\"",
    "build": "cd client && npm install --only=dev && npm install",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install",
    "setup": "npm install && cd client && npm install"
  }
}
